# 🚀 Pulso-AI Development Environment Variables
# Copia este archivo como .env y ajusta los valores según tu entorno

# =====================================
# 🗄️ DATABASE CONFIGURATION
# =====================================

# PostgreSQL Database
DATABASE_URL=postgresql://pulso_ai:dev_password_2024@localhost:5432/pulso_ai_dev
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=pulso_ai_dev
POSTGRES_USER=pulso_ai
POSTGRES_PASSWORD=dev_password_2024

# Database Connection Pool
DATABASE_POOL_SIZE=10
DATABASE_MAX_OVERFLOW=20
DATABASE_POOL_TIMEOUT=30

# =====================================
# 🔴 REDIS CONFIGURATION  
# =====================================

REDIS_URL=redis://:dev_redis_2024@localhost:6379/0
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=dev_redis_2024
REDIS_DB=0

# Redis Cache Settings
REDIS_TTL_DEFAULT=3600
REDIS_TTL_SESSION=86400
REDIS_TTL_DASHBOARD=300

# =====================================
# 🔐 SECURITY & AUTHENTICATION
# =====================================

# JWT Settings
JWT_SECRET_KEY=dev_super_secret_key_change_in_production
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=60
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# API Keys (para development)
API_SECRET_KEY=dev_api_secret_2024
ADMIN_API_KEY=dev_admin_key_2024

# CORS Settings
CORS_ORIGINS=http://localhost:3000,http://localhost:3001,http://127.0.0.1:3000
CORS_METHODS=GET,POST,PUT,DELETE,OPTIONS
CORS_HEADERS=*

# =====================================
# 🌐 APPLICATION SETTINGS
# =====================================

# Environment
ENVIRONMENT=development
DEBUG=True
LOG_LEVEL=DEBUG

# Application URLs
BACKEND_URL=http://localhost:8000
FRONTEND_URL=http://localhost:3000
API_V1_PREFIX=/api/v1
GRAPHQL_ENDPOINT=/graphql

# File Upload
MAX_FILE_SIZE_MB=100
ALLOWED_FILE_TYPES=csv,xlsx,json

# =====================================
# 🏢 MULTI-TENANT SETTINGS
# =====================================

# Client Configuration
DEFAULT_CLIENT_ID=movistar-peru
CLIENT_ISOLATION_MODE=namespace
CLIENT_DOMAIN_ROUTING=subdomain

# BigQuery Settings (para Movistar)
BIGQUERY_PROJECT_ID=mibot-222814
BIGQUERY_DATASET=BI_USA
BIGQUERY_CREDENTIALS_PATH=./secrets/bigquery-dev-credentials.json

# =====================================
# ⚡ PERFORMANCE & CACHING
# =====================================

# Polars ETL Settings
POLARS_STREAMING=True
POLARS_THREAD_POOL_SIZE=4

# GraphQL Settings
GRAPHQL_INTROSPECTION=True
GRAPHQL_PLAYGROUND=True
GRAPHQL_QUERY_DEPTH_LIMIT=10
GRAPHQL_QUERY_COMPLEXITY_LIMIT=1000

# Dashboard Caching
DASHBOARD_CACHE_TTL=300
CROSS_FILTER_CACHE_TTL=60
METRICS_CACHE_TTL=900

# =====================================
# 📊 MONITORING & OBSERVABILITY
# =====================================

# Logging
LOG_FORMAT=json
LOG_FILE_PATH=./logs/pulso-ai.log
LOG_ROTATION_SIZE=10MB
LOG_RETENTION_DAYS=30

# Metrics
METRICS_ENABLED=True
METRICS_ENDPOINT=/metrics
HEALTH_CHECK_ENDPOINT=/health

# Sentry (Error Monitoring)
SENTRY_DSN=
SENTRY_ENVIRONMENT=development
SENTRY_TRACES_SAMPLE_RATE=0.1

# =====================================
# 🔧 DEVELOPMENT TOOLS
# =====================================

# Hot Reload
BACKEND_RELOAD=True
FRONTEND_HOT_RELOAD=True

# Development Helpers
MOCK_DATA_ENABLED=True
SEED_DATA_ON_STARTUP=True
PROFILING_ENABLED=True

# Testing
TEST_DATABASE_URL=postgresql://pulso_ai:dev_password_2024@localhost:5432/pulso_ai_test
TEST_REDIS_URL=redis://:dev_redis_2024@localhost:6379/1

# =====================================
# 📱 EXTERNAL SERVICES (Development)
# =====================================

# Email (Development - usar MailHog o similar)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USERNAME=
SMTP_PASSWORD=
FROM_EMAIL=noreply@pulso-ai.dev

# Webhooks (para testing)
WEBHOOK_SECRET=dev_webhook_secret_2024
WEBHOOK_TIMEOUT=30

# File Storage (Local para development)
STORAGE_TYPE=local
STORAGE_PATH=./storage
STORAGE_URL_EXPIRY=3600

# =====================================
# 🏗️ CLIENT-SPECIFIC OVERRIDES
# =====================================

# Movistar Peru (BigQuery)
MOVISTAR_BIGQUERY_PROJECT=mibot-222814
MOVISTAR_BIGQUERY_DATASET=BI_USA
MOVISTAR_BIGQUERY_TABLE=dash_P3fV4dWNeMkN5RJMhV8e_vw_operativo

# Claro Colombia (PostgreSQL) - Para future implementation
CLARO_DATABASE_URL=postgresql://claro_user:claro_pass@localhost:5433/claro_db

# Tigo Guatemala (MySQL) - Para future implementation  
TIGO_DATABASE_URL=mysql://tigo_user:tigo_pass@localhost:3306/tigo_db

# =====================================
# 📝 NOTES
# =====================================

# 1. NUNCA comitees el archivo .env con secrets reales
# 2. Para producción, usa variables de entorno del sistema o secrets management
# 3. Ajusta CORS_ORIGINS según tu setup de frontend
# 4. Las passwords de desarrollo son seguras solo para development local
# 5. Para testing, usa valores diferentes en TEST_* variables
